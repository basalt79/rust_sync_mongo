# Rust and Mongo


## Mongo
* Access a mongo database and do some `CRUD` operation


## Bson / Json
* struct to json
* json to bson
* bson to json
* json to struct

## Equality of structs
* by `#[derive(PartialEq)]`

